<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SocialSphere" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorPrimary">@color/amazon_primary</item>
        <item name="colorPrimaryDark">@color/amazon_primary_dark</item>
        <item name="colorAccent">@color/textcolor</item>
    </style>

    <style name="Theme.SocialSphere" parent="Base.Theme.SocialSphere" />
    <style name="round" parent="">
        <item name="cornerSize">50%</item>
    </style>
</resources>